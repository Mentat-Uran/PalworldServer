# Daily operations

Use the Web Console for save, backup, announcements, player management,
settings, logs, and runtime status. The normal operations use REST. The legacy
RCON console appears only when explicitly enabled for compatibility.

Before a restart or update, notify players, save the world, create a backup,
and confirm that the active runtime is the one you intend to operate.
